{
  "name": "voicepet-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5210 --strictPort",
    "build": "vite build",
    "capture": "vite build --outDir dist-preview && node tools/capture.js",
    "playwright:install": "playwright install chromium",
    "test": "node --test"
  },
  "dependencies": {
    "three": "^0.186.0"
  },
  "devDependencies": {
    "playwright": "^1.63.0",
    "vite": "^8.3.0",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
